# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

Added `ListProduct` component

Refactoring the `ListProduct` component to use VTEX sort options

Refactored to componentize the app adding `ProductCard`, `Products`, `ProductSearchInput`, `ProductSortSelect` components

Added `ListProductsContext` as a provider for entire app

Refactoring the directory structure
